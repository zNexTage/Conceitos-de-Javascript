function soma(n1, n2) {
    return n1 + n2;
}

function setReplace(frase, nome, novoNome) {
    return frase.replace(nome, novoNome);
}

function validaIdade(idade) {
    return idade >= 18;
}

alert(soma(10, 5));
alert(setReplace("Acelera Senna", "Senna", "Barrichello"));
alert(validaIdade(age));